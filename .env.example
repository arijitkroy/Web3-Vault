# GetBlock RPC URL for Sepolia (same as in getblock.config.js)
SEPOLIA_RPC_URL=https://go.getblock.io/YOUR_TOKEN/

# Private key of the deployer wallet (MetaMask export → never commit this file)
DEPLOYER_PRIVATE_KEY=your_private_key_here
